const types = {
    // ...
}

const state = {
    files: {
        selected: null,
        items: {},
        order: {},
        orderBy: 'date',
        orderDirection: 'ASC'
    },
    contentTypes: {},
    lastSynced: 0
}

const mutations = {
    // ...
}

const actions = {
    // ...
}

const getters = {
    // ...
}

export default {state, mutations, actions, getters, types}
